from setuptools import setup, find_packages


if __name__ == '__main__':
    setup(
        name='aiida_optimizer_demo',
        packages=['aiida_optimizer_demo'],
        include_package_data=True,
        entry_points={},
    )
